# Bot tests

Node.js built-in test runner (`node --test`). No external test framework — runs out of the box with Node 20+.

```
npm test
```

Each file under `test/` is a standalone test suite. The validation layer is the highest-value target: bad input there can corrupt the shared license dataset.
